<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mohanteja.vizagporttrust">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Main activity -->
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity
            android:name=".ElectronicsActivity"
            android:label="ELECTRONICS" />


        <activity android:name=".BGLocosActivity"
            android:label="BG LOCOS"/>

        <activity android:name=".PowerHouseActivity"
            android:label="POWER DISTRIBUTION"/>

        <activity android:name=".FCSActivity"
            android:label="FLOATING CRAFT SECTION"/>

        <activity android:name=".LightingActivity"
            android:label="LIGHTING"/>
        <activity android:name=".CraneActivity"
            android:label="CRANES"/>
        <activity android:name=".PumpHouseActivity"
            android:label="PUMP HOUSE"/>



    </application>



</manifest>